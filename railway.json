{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install --frozen-lockfile && bun --bun run build"
  },
  "deploy": {
    "startCommand": "cd apps/web && bun --bun run start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
